{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "command": "pip install -r requirements.txt && python app.py"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app.py"
    }
  ]
}
